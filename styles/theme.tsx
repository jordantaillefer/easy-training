import { DefaultTheme } from 'styled-components';

export const theme: DefaultTheme = {
  colors: {
    primary: 'var(--charcoal)',
    secondary: 'var(--dark-side-blue)',
    ternary: 'var(--dark-side-blue)',
    backgroundMain: 'var(--background-main)'
  },
};
